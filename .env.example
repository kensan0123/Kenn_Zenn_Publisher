# ==========================================
# Docker Secrets Configuration (Recommended for Production)
# ==========================================
# Create the following files:
# - secrets/github_pat.txt
# - secrets/openai_api_key.txt
# - secrets/anthropic_api_key.txt
#
# These will be automatically mounted to /run/secrets/ by docker-compose.yml
# entrypoint.sh will automatically read from these files

# ==========================================
# Environment Variables (For Development)
# ==========================================
# If not using Secrets files, you can set the following environment variables directly

# OpenAI API Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# GitHub Configuration (For Zenn Publishing)
# GitHub Personal Access Token (requires repo permission)
GITHUB_PAT=ghp_your-github-personal-access-token
GITHUB_USER=your-github-username

# Git Configuration (Used for commits)
USER_NAME=Your Name
USER_EMAIL=your.email@example.com

# Application Configuration
# Path inside Docker container
ARTICLE_DIR=/app/articles

# Database Configuration
POSTGRES_USER=your-db-user-name
POSTGRES_PASSWORD=your-db-password
POSTGRES_DB=your-db-name