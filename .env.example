# ==========================================
# Docker Secrets 設定（本番環境推奨）
# ==========================================
# 以下のファイルを作成してください：
# - secrets/github_pat.txt
# - secrets/openai_api_key.txt
# - secrets/anthropic_api_key.txt
#
# docker-compose.ymlで自動的に /run/secrets/ にマウントされます
# entrypoint.shが自動的にファイルから読み込みます

# ==========================================
# 環境変数設定（開発環境用）
# ==========================================
# Secretsファイルを使わない場合は、以下の環境変数を直接設定できます

# OpenAI API設定
OPENAI_API_KEY=sk-your-openai-api-key-here

# GitHub設定（Zenn公開用）
# GitHub Personal Access Token (repo権限が必要)
GITHUB_PAT=ghp_your-github-personal-access-token
GITHUB_USER=your-github-username

# Git設定（コミット時に使用）
USER_NAME=Your Name
USER_EMAIL=your.email@example.com

# アプリケーション設定
# Dockerコンテナ内のパス
ARTICLE_DIR=/app/articles

POSTGRES_USER=your-db-user-name
POSTGRES_PASSWORD=your-db-password
POSTGRES_DB=your-db-name